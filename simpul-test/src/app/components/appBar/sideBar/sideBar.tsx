"use client";
import React, { useContext, useState } from "react";
import styles from "./style.module.css";
import Container from "@mui/material/Container";

const SideBar = () => {
  return <Container className={styles.sidenav}></Container>;
};

export default SideBar;
